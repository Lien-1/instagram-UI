import React from "react";
import { Text, View, SafeAreaView } from "react-native";

const Profile = () => {
  return (
    <SafeAreaView>
      <Text>Profile</Text>
    </SafeAreaView>
  );
};

export default Profile;
